# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# The following keys must be escaped otherwise they will not be retrieved by
# the default I18n backend:
#
# true, false, on, off, yes, no
#
# Instead, surround them with single quotes.
#
# en:
#   'true': 'foo'
#
# To learn more, please read the Rails Internationalization guide
# available at https://guides.rubyonrails.org/i18n.html.

en-GB:
  helpers:
    submit:
      create: "Add a %{model}"
      update: "Confirm changes"
  activerecord:
    models:
      country:
        one: Country
        other: Countries
      network:
        one: Network
        other: Networks
      payg_plan:
        one: Pay As You Go Plan
        other: Pay As You Go Plans
      payg_plan_cost:
        one: PAYG Cost
        other: PAYG Costs
      membership_plan:
        one: Membership Plan
        other: Membership Plans
    attributes:
      country:
        countryCode: Country Code
      network:
        website_url: Website URL
        pricing_url: Pricing URL
        android_app_url: Android App URL
        ios_app_url: iOS App URL
      payg_plan:
        plan_url: Plan URL
        ended_at: Ended
      payg_plan_cost:
        cost_per_kwh: Price per kWh
        cost_per_kwh_currency: Currency
        cost_per_minute: Price per minute
        cost_per_minute_currency: Currency
        ended_at: Ended
      membership_plan:
        plan_url: Plan URL
        cost: Cost
        cost_currency: Currency
        ended_at: Ended
        cost_frequency: Cost Frequency
      membership_plan/cost_frequencies:
        once: One-off
        annually: Annually
        quarterly: Quarterly
        monthly: Monthly
        weekly: Weekly
      membership_plan/cost_frequencies_per_period:
        once: One-off
        annually: per year
        quarterly: per quarter
        monthly: per month
        weekly: per week
